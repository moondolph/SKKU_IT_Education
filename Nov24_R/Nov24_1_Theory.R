# R ?
#   => R은 통계를 위한 프로그래밍 언어
#   => 또한, 수치를 계산하고, 그래프 등의 시각화 기능을 가진 언어 

# R을 쓰는 이유 ?
#   => 기본적인 통계와 시각화 가능 -> R에 통계용어가 꽤나 많음
#   => 통계, 시각화 패키지들이 존재함
#   => 무료, 오픈소스 
#   => Data Frame이라는 매력적인 자료형이 존재함 

# DataFrame ? 
#   => Table 형식의 데이터를 표현하고, 다루는 방식
#   => 여러 행, 열로 구성이 된 2차원 형식
#   => RDBMS(ex: Oracle)의 테이블의 느낌
#   => 정수, 실수, 문자열, ... 의 여러 데이터를 처리할 수 있음

# 단축키
#   한 줄 실행 : ctrl + enter
#   전체 실행 : ctrl + shift + enter
#   한 줄 지우기 : ctrl + d
#   console clear : ctrl + l
#   주석처리 : ctrl + shift + c
#     => Tools 탭 - Modify Keyboard Shortcuts - comment - Ctrl + /로 바꾸기
#   기능목록 : ctrl + space
#   자동완성 : tab
# 
# 